This SQL data cleaning project was condected on a nashville housing dataset.
I used CTE, window function (partition by) to delete dupricates 
and alter table (drop) function to delete unused columns 
